version: '3'

services:
  app:
    # docker-compose build
    # docker-compose run app sh -c "django-admin.py startproject app ." või
    # docker-compose run app sh -c "python manage.py startproject app ."git
    build:
      context: .
    ports:
      - '8000:8000'
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"
